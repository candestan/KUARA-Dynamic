# Benchmarks

Planned benchmarks:

- rule load/parse/compile time
- scan throughput (small/medium/large inputs)
- many-rule scaling (1k, 10k, 20k)
- wildcard-heavy vs literal-heavy profiles
- explain-mode overhead

Comparative claims against YARA are intentionally deferred until reproducible benchmark scripts are added.

